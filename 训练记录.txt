921a 100个epoch，起始学习率1e-3，中间1e-4，最后1e-5. 按照三分之一epoch递进。
网络按照finetune进行调优，起始网络用的是917c的参数（loss 1.50）。网络开始loss直接跳到2点几
，然后快速下降，基本每一个epoch可以下降0.1，然后逐渐稳定，最后网络稳定到1.55。还不如与训练模型的loss
分析认为学习率设置过大，破坏了主干网络的参数，导致了这样的结果。
921b 300个epoch，起始学习率1e-4，中间1e-5，最后1e-6，按照三分之一epoch递进。
网络按照finetune进行调优，参数仍然使用917c的参数（loss 1.50）。同时关闭了数据集的shuffle